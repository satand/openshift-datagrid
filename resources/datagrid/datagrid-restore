apiVersion: infinispan.org/v2alpha1
kind: Restore
metadata:
  name: my-restore
  namespace: datagrid-demo
spec:
  backup: my-backup
  cluster: infinispan # It's the target-cluster and it may be a different cluster from the source one
  # # Optional restored resources
  # resources:
  #     templates:
  #       - distributed-sync-prod
  #       - distributed-sync-dev
  #     caches:
  #       - cache-one
  #       - cache-two
  #     counters:
  #       - counter-name
  #     protoSchemas:
  #       - authors.proto
  #       - books.proto
  #     tasks:
  #       - wordStream.js